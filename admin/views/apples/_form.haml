- error = @apple.errors.include?(:name)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :name, :class => 'control-label'
  .controls
    =f.text_field :name, :class => 'input-xlarge input-with-feedback', :autofocus => true
    %span.help-inline=error ? f.error_message_on(:name, :class => 'text-error') : pat(:example)
- error = @apple.errors.include?(:tweet_id)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :tweet_id, :class => 'control-label'
  .controls
    =f.text_field :tweet_id, :class => 'input-xlarge input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:tweet_id, :class => 'text-error') : pat(:example)
- error = @apple.errors.include?(:tweet)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :tweet, :class => 'control-label'
  .controls
    =f.text_field :tweet, :class => 'input-xlarge input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:tweet, :class => 'text-error') : pat(:example)
- error = @apple.errors.include?(:tweeted_at)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :tweeted_at, :class => 'control-label'
  .controls
    =f.text_field :tweeted_at, :class => 'input-xlarge input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:tweeted_at, :class => 'text-error') : pat(:example)

.form-actions
  =f.submit pat(:save), :class => 'btn btn-primary'
  &nbsp;
  =f.submit pat(:save_and_continue), :class => 'btn btn-info', :name => 'save_and_continue'
  &nbsp;
  =link_to pat(:cancel), url(:apples, :index), :class => 'btn'
