.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:apples, :index)
      %li.active=link_to pat(:new), url(:apples, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:apple)
    .inner
      -form_for :apple, url(:apples, :create), :class => :form do |f|
        =partial "apples/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")
